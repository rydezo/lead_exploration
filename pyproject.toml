[project]
name = "lead-exploration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["bakery>=1.0.0"]

[tool.pyright]
"include" = ["src"]
"typeCheckingMode" = "strict"
